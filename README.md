# Agent workspace smoke fixture

This repository is a disposable Node.js backend and frontend fixture for testing
an isolated coding-agent workspace.

`agent-workspace.yaml` defines setup, service readiness, browser verification,
artifact paths, and cleanup. The fixture contains no production application
code, credentials, or customer data.
